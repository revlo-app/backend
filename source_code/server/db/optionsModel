const mongoose = require("mongoose");
const OptionsSchema = new mongoose.Schema({
    

registerAlerts: {
    type: Boolean,
    default: true
},



})
  
module.exports = mongoose.model.Options || mongoose.model("Options", OptionsSchema);